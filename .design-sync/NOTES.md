# design-sync notes

- Owner (2026-09-08): tokens and styles must come from the LATEST unified design-system work used on the Home, Solar, HamClock wall, and Shack pages (DS-01..DS-15 programme, station palette). Do not source older token sets.
- Repo is a Vite SPA application, not a component library; no Storybook. Package shape.

## 2026-09-08 initial curated-set sync (132 components, validate exit 0)

**Barrel entry / config**: `.design-sync/ds-entry.tsx` re-exports the curated set from `src/components/{ui,station-ui,solar,home,shack,dx,layout}` plus the HamClock wall chrome + 22 wall tiles, as named exports via `@/` imports (per repo convention). `componentSrcMap` in `config.json` pins every one of them (132 entries) so the package adapter doesn't need to fuzzy-find. `package.json` at repo root has no `main`/`module`/`exports`, so `exportedNames()` returns nothing and the component list is exactly `componentSrcMap`'s keys - working as intended for an app repo, not a published package.

**Name collisions resolved in the barrel** (both real exports, not renamed in src/): `station-ui/Feedback.tsx`'s `Badge` re-exported as `StationBadge` (collides with `ui/Badge.tsx`'s `Badge`); `station-ui/Overlays.tsx`'s `Dialog` re-exported as `StationDialog` (collides with `ui/AccessibleDialog`'s naming space / avoids ambiguity with future `Dialog` additions).

**Provider** (`cfg.provider.component = "DsProvider"`, defined in `ds-entry.tsx`): `QueryClientProvider` (retry off) → `MemoryRouter` → `StationProvider` (station-ui theme/token context - required by every station-ui/, shack/, HamClock-tile component, which read `--su-*` CSS vars set by this provider) → `HomeLocationProvider` (required by `HomeActivity`, `HomeDaylight`, `HomeForecastStrip`, `HomeSolar`, `HomeWeather` - they call `useHomeLocation()` and throw without it). `ds-entry.tsx` also does a bare `import "@/stores/themeStore"` at module top, matching `src/App.tsx`'s pattern - the store's module-level side effect calls `applyThemeToDocument()` to paint the persisted theme/accent CSS custom properties onto `<html>` before anything renders. No `ThemeProvider`/`ToastProvider` component exists in this app (theme is a Zustand-store side effect, not a React context); no such provider was needed beyond the above.

**Bundler workarounds - all config-level, nothing under `src/` touched, nothing in `.ds-sync/lib/` forked:**

1. **Bare directory imports (`@/components/ui`, `@/lib/db`, `@/lib/contest`, `@/lib/themes`, `@/components/station-ui`, `@/components/map/hamclock/wall/controls`, + ~40 more barrel dirs).** `lib/bundle.mjs`'s `tsconfigPathsPlugin` resolves the `@/*` wildcard by trying extensions in order `['', '.ts', '.tsx', ..., '/index.ts', ...]` - since `''` is tried first and `existsSync()` doesn't distinguish files from directories, a bare `@/components/ui` import resolves to the *directory itself* before ever trying `/index.ts`, and esbuild fails with `Cannot read file "...": is a directory`. Fixed by writing a repo-specific tsconfig, `.design-sync/tsconfig.ds-sync.json` (generated once by scanning `find src -name index.ts -o -name index.tsx`), that lists an **exact** `paths` entry for every `src/**/index.ts(x)` directory pointing directly at its index file, inserted *before* the generic `@/*` wildcard entry (object key order = rule try order in the plugin). `config.json`'s `tsconfig` field points at this file instead of the repo's real `./tsconfig.json`. If a NEW barrel directory is added under `src/` later, re-run the same `find` + regenerate this file (see the node one-liner in build history / just re-derive it) or the same class of error will resurface for it.
2. **`virtual:pwa-register/react`** (imported by `ui/PWAUpdatePrompt.tsx`) is a Vite-plugin-pwa virtual module that only exists inside a real Vite build; esbuild can't resolve it. Same `tsconfig.ds-sync.json` `paths` mechanism redirects that bare specifier to `.design-sync/shims/virtual-pwa-register-react.ts`, a tiny local shim whose `useRegisterSW()` always returns `needRefresh: [false, setState]` (matches the hook's real "nothing pending" default) so `PWAUpdatePrompt` renders its natural empty state instead of crashing.
3. **`SpotRow` (`dx/DXSpotList/SpotRow.tsx`) pulled in `@react-three/fiber` + `@react-three/drei` + `react-reconciler`, which crashed the ENTIRE bundle on load** (`[SCHEDULER_MISSING]`, and every one of the 132 components failed `[BUNDLE_EXPORT]` in the same run - not just SpotRow's own card - because `@react-three/fiber` eagerly constructs its reconciler at module top-level, and SpotRow statically imports two tiny pure formatters, `getSpotAgeInfo`/`formatSpotAge`, from `map/LiveSpotArcs.tsx`, a full Three.js globe-overlay component esbuild can't tree-shake around due to top-level side effects). Fixed the same way: `tsconfig.ds-sync.json` redirects the bare specifier `@/components/map/LiveSpotArcs` to `.design-sync/shims/live-spot-arcs-age.ts`, a verbatim port of just those two pure Date-math functions (no React/Three deps). **Re-sync risk**: if `LiveSpotArcs.tsx`'s age-bucket thresholds/format ever change, this shim goes stale silently - diff it against the real file on future re-syncs of `SpotRow`.
4. **`cssEntry` local `@import` resolves relative to `ds-bundle/` (`OUT`), not relative to the importing file.** `package-build.mjs` appends `cfg.cssEntry`'s RAW file text verbatim into `_ds_bundle.css` (no `@import` resolution of its own); `package-validate.mjs`'s `[CSS_IMPORT_MISSING]` check then does `existsSync(join(OUT, importPath))`. `.design-sync/ds.css` (cssEntry) therefore imports the compiled app stylesheet as `@import "../.design-sync/.cache/app.css";` (one level up from `ds-bundle/`, not `./.cache/app.css` as the naive recipe would suggest) so it resolves correctly against `OUT`. **Re-sync risk / upload blocker**: this reaches *outside* `ds-bundle/` via `../`, which is fine for local `build`+`validate` but WILL dangle if `ds-bundle/` is ever uploaded/copied standalone (claude.ai/design only receives the `OUT` tree). Before the first real upload, either (a) have the build step physically copy `.design-sync/.cache/app.css` into `ds-bundle/.cache/app.css` post-build, or (b) inline app.css's resolved content directly into `ds.css` instead of `@import`-ing it. Not yet resolved - flagging for whoever does the §5 upload pass.
5. **`cfg.tokensGlob`** is currently a **no-op**: `lib/css.mjs`'s `copyTokens()` only does anything when `cfg.tokensPkg` is also set (it looks up `join(nodeModules, tokensPkg)/package.json`), and this repo has no separate tokens npm package - it's a single-package Vite app, not a monorepo. `tokensGlob` is left set (`src/styles/globals.css`, `hamclock-themes.css`, `hamclock.css`, `home.css`) as documentation of intent per the task brief, but it ships nothing; token delivery instead happens entirely through `cssEntry` → the compiled `app.css` (which already contains those files' compiled output, since Vite bundled them into `dist/assets/*.css`). No `tokens/` dir appears in `ds-bundle/`. If a real npm-scoped tokens package is ever split out of this repo, wire `tokensPkg` then.

**Known render warns (non-blocking `[RENDER_BLANK]`, PNG <5KB, all on unauthored floor-card components - expected, not chased):** `ToggleSwitch`, `FeedlineLossSparkline`, `BandRow`, `SolarDisclosure`, `ActionBar`, `Checkbox`, `Disclosure`, `EmptyState`, `ProvenanceBadge`, `StationBadge`. These are small/thin controls or data-driven components (sparkline, band row) rendering with the `.d.ts`-derived crash-prevention default props (empty arrays / falsy flags) and no authored composition yet - expected until `.design-sync/previews/<Name>.tsx` is authored for them. 65/132 components are showing the plain typographic floor card (no authored preview) - also expected, not a failure.

**No components were dropped.** All 132 in the curated brief bundled and validated (122 clean, 10 in the known-warns list above, 0 `[RENDER]` root-empty failures).

**Chromium / render check**: ran for real (not `--no-render-check`). `playwright@1.61.1` + `@playwright/test` already installed in this worktree's `node_modules` (repo test suite dependency); its pinned chromium build (revision 1228) was already cached at `~/Library/Caches/ms-playwright/chromium-1228` (macOS cache path - NOT `~/.cache/ms-playwright/`, which doesn't exist on this machine). No install needed. `package-validate.mjs` resolves the bare `playwright` import by walking up from `.ds-sync/` to the repo root `node_modules/playwright`, which is present.

**Not yet done (explicitly out of scope for this pass, per the task brief)**: no `.design-sync/previews/*.tsx` authored, no absolute grading (§4.3), no `.review.html` human review pass, no upload (§5). Next agent picking up preview authoring should start from the "Known render warns" list above plus the 65 floor-card components.

## 2026-09-08 orchestrator fixes after the first clean build

- **CSS chain (fixed; supersedes workaround #4 above).** `cfg.cssEntry` now points straight at the compiled app stylesheet `.design-sync/.cache/app.css` (written by `buildCmd`); the old `.design-sync/ds.css` shim is deleted. Reason: `package-build.mjs` appends `cssEntry`'s raw text to the END of `_ds_bundle.css`, after the component CSS esbuild scraped, so any `@import` inside it sits after other rules and browsers ignore it (CSS requires `@import` first). The first build's `_ds_bundle.css` was 42 KB (station-ui.css only); it is now ~426 KB with the full Tailwind/globals/theme output. `_ds_bundle.css` therefore contains station-ui.css twice (esbuild scrape + app.css copy) - harmless, later copy wins with the app's own order.
- **Fonts (fixed).** Inter / JetBrains Mono / Orbitron are loaded by `index.html` from Google Fonts in the app; designs only see `styles.css`'s import closure, so they ship locally: `node .design-sync/fetch-fonts.mjs` downloads the latin woff2 files into `.design-sync/fonts/` and writes `fonts.css`; `cfg.extraFonts = [".design-sync/fonts/fonts.css"]` copies them to `ds-bundle/fonts/` and `styles.css` imports `./fonts/fonts.css` before `_ds_bundle.css`. Google serves the variable file for each family, so the 400/500/600 files are byte-identical - expected. Re-run `fetch-fonts.mjs` only if `index.html`'s font link changes.
- **Classic/brass theme fonts (shipped 2026-09-08, owner ask).** `src/styles/hamclock-themes.css` names Cormorant Garamond (`--hc-font-display`, weights 600/700 via `--hc-display-weight`) and IBM Plex Mono (`--hc-font-mono`, 400-600) for the classic and brass themes. The app itself never loads them (only Inter / JetBrains Mono / Orbitron are linked), so production renders their fallbacks; the design project ships them anyway so the themed cards look as designed. `fetch-fonts.mjs` now pulls both families (500/600/700 and 400/500/600). "Hoefler Text" is a macOS system font, not on Google Fonts, and stays a fallback; a residual `[FONT_MISSING]` naming only it is expected.
- **`.d.ts` props (fixed).** `lib/dts.mjs` only parses `**/*.d.ts`; an app repo ships none, so every `<Name>Props` came out as `[key: string]: unknown` (the design agent would have had no API contract). Fix: `bash .design-sync/emit-types.sh` (run before `package-build.mjs`; add it to the build recipe) emits a declaration tree with `tsc -p .design-sync/tsconfig.types.json` into gitignored `dist/types/`, writes a stub `dist/types/package.json` (`name` + `types: index.d.ts`) so `loadDts` treats it as the package root, and an `index.d.ts` that re-exports the barrel. `findTypesRoot` picks `dist/types` because the root `package.json` has no `types` field. The script also rewrites the barrel's `"@/..."` re-exports to `"../src/..."` because ts-morph runs without tsconfig `paths` and cannot follow the alias - without that rewrite, the ~75 components whose props are inline (no `<Name>Props` interface) still fall to the stub. One `TS4058` declaration diagnostic (`useDXCluster` return type) is expected and harmless; the script tolerates it. Delete `node_modules/.tmp/tsconfig.types.tsbuildinfo` if the emit ever appears to skip files (incremental build info is removed by the script for that reason).
- **Build recipe (current):** `npm run build && mkdir -p .design-sync/.cache && cat dist/assets/*.css > .design-sync/.cache/app.css && bash .design-sync/emit-types.sh`, then `node .ds-sync/package-build.mjs --config .design-sync/config.json --node-modules ./node_modules --entry .design-sync/ds-entry.tsx --out ./ds-bundle`, then `node .ds-sync/package-validate.mjs ./ds-bundle`.
- **Composition source for previews:** `src/pages/design-system/DesignSystemPage.tsx` is an in-repo showcase page with author-written compositions for the station-ui set (Buttons, Notice tones, KeyValueList, Table, Tabs, Dialog, fields). Port from there first; solar/home/wall tiles have no showcase and compose from source.
- **Wave learnings (station-ui-A, folded 2026-09-08):** `Field` is a render-prop leaf (`children: (control) => ReactNode`) with no real call site in `src/` (the `<Field>` in `qso/QSODetailModal.tsx` is a different same-named component) - its preview uses native inputs spread with `{...control}` and `className="su-input"`. Previews cannot import lucide-react; icon-bearing previews carry small inline `<svg>` helpers. Image-bearing previews use `data:image/svg+xml` URIs so capture never needs the network.
- **Wave learnings (station-ui-B, ui-A, folded 2026-09-08):** `StationDialog` portals to `document.body` but the capture harness contained it in the card, no `cardMode` override needed. `ActionBar` has no horizontal padding by design (real usage nests it in Surface/Section); its preview wraps in `<Surface>`. `Tooltip`/`GlossaryTooltip` are hover-only with internal open state, so cards show the trigger only. `HealthStatusIndicator` renders whatever the live health hook resolves outside the app shell ("Systems OK"), not steerable by props. `ImageUploadButton` thumbnail needs an IndexedDB image, so cards show the upload trigger only. `UndoToast` stays on the floor card: its only prop is `className` and content comes from `useUndoStore` (returns `null` with no active toast). `PanelCard` and `DataSourceError` have no call sites in `src/`; previews composed from source.
- **Wave learnings (tiles-A, folded 2026-09-08):** wall tiles take zero props (optional `title` on AlertsTile/EmcommTile/ForecastMatrixTile/MufTile) and read stores/hooks only, so cards show the honest "no station / awaiting feed" states from wall spec §7. Width cells (Wall 340px, Narrow 220px) are real variants because `.hc-tile` is a `container-type: inline-size` container with `cqi`-clamped hero tokens. Never wrap a lone tile in `.hc-wall[data-density="desk"]`: that selector carries the wall grid rows and misplaces a single child; desk scaling comes from the token being redeclared on that block, not from `--hc-scale` inheritance. `:root` already carries the pulse theme tokens, so a bare `<div style={{width}}>` wrapper is enough.
- **Wave learnings (home-shack, folded 2026-09-08):** `useHomeLocation()` resolves null in the sandbox (no auth, no station chain, empty `propulse-home-location-v2:*` key), so HomeDaylight/HomeWeather/HomeForecastStrip/HomeActivity show their honest "no location" states. HomeSolar/HomeForecastStrip/HomeBriefing take `model: ReturnType<typeof useSolarModel>`; previews pass plain object literals with only the fields each destructures. FeedlineLossSparkline reads `useUserFeedlines()` (empty) and renders its designed dashed placeholder. EquipmentSection mounts the full SetupWizard on an empty inventory and runs past the card fold (cropped, not broken). EquipmentHeroCard's portal overlay stays contained without a cardMode override.
- **Wave learnings (ui-B, folded 2026-09-08):** floor cards with no forcing prop: ConnectivityBadge (store `connectivity !== "lan"`), OfflineIndicator (`navigator.onLine`), SyncStatusIndicator (empty sync queue), PWAUpdatePrompt (the `virtual:pwa-register/react` shim hardcodes `needRefresh:false`), PWAInstallPrompt (`beforeinstallprompt` simulation did not open the banner in two tries). Front-door DOM simulation is fine for internal-state components: ShortcutsHelpModal (click second `[role="tab"]`), CommandPalette and ReferencePanel (native `HTMLInputElement` value setter + `input` event). OnboardingTour modal steps use inline `translate(-50%,-50%)` with `position: fixed`, contained by the cell's `translateZ(0)`; grid cells misplace it, so `overrides.OnboardingTour = { cardMode: "single", primaryStory: "WelcomeStep" }` (needs a full build to re-stamp).
- **Wave learnings (solar-dx, folded 2026-09-08):** upstream bug, not fixed here: `LoadingSpinner size="lg"` uses `border-3`, which Tailwind does not define, so the large spinner is invisible (PropagationIndex `loading` cell dropped for a third data state). The emitted `PrimaryMetrics.d.ts` loses the `| null` unions from the source props; previews pass numbers plus `loading`. PredictionsCard and SolarImageCard read hooks/network and show their real unavailable states (PredictionsCard's two cells render identically). FloatingPanel's `position: fixed` stayed contained; no cardMode overrides.
- **Wave learnings (tiles-B-wall, folded 2026-09-08):** wall chrome sizes off viewport-relative tokens (`--hc-rail: calc(18vw * var(--hc-scale))`, vh hero fonts), which resolve too small in the 900×700 capture viewport. Set the token on the preview wrapper instead of squeezing the container: HamClockRail uses `--hc-rail: 300px`, HamClockWallHeader's DeskScale cell uses `--hc-scale: 0.72` (narrowing the container alone made "NO CALL" collide with the clock digits). The single-story capture is a hard 900×700 crop, not a scroll or scale; anything larger is silently truncated.
- **OnboardingTour modal steps (2026-09-08):** the down-right offset is a production bug, not a capture artefact. The tooltip carries `animate-fade-in-up` (`fadeInUp 0.5s ease-out both`); the animation's final `transform: translateY(0)` outranks the inline `translate(-50%, -50%)`, so the modal steps never centre. Filed as a GitHub issue; graded needs-work with the preview kept (it renders exactly what ships). Re-grade after the fix lands.

## 2026-09-08 close-out (project b5bd52bb-3dae-49e3-a9d9-0ee4fd4feae6, incremental path)

- **Final state uploaded:** 711 files (README, `_ds_bundle.js/.css`, `styles.css`, `_vendor/` ×2, `fonts/` ×9, `guidelines/` ×43, `_preview/` ×125, `components/` 132 dirs × 4) plus `_ds_needs_recompile` and `_ds_sync.json` written last. Reconciliation deletes: none (remote matched local). `_adherence.oxlintrc.json` and `_ds_manifest.json` in the project are app-generated; leave them alone.
- **Grades:** 132/132 render; 124 authored previews graded good; 1 needs-work kept on purpose (OnboardingTour, production bug #636); 7 floor cards with no forcing prop (ConnectivityBadge, OfflineIndicator, SyncStatusIndicator, PWAUpdatePrompt, PWAInstallPrompt, UndoToast, StationProvider - a provider, nothing to show).
- **Validator:** exit 0, 56 non-blocking warnings (RENDER_BLANK on the floor cards, FONT_MISSING substitutes recorded above, GRID_OVERFLOW resolved via `overrides.*.cardMode = "column"` for HamClockTile / HamClockWallControls / HamClockWallHeader / WallStatus).
- **Guidelines narrowed** (`guidelinesGlob`) to `docs/designs/*.md` + `card-design-best-practices.md` + `hamclock-tile-system.md`; the earlier broad glob dragged in plans and ML docs the design agent has no use for.

### Re-sync risks (read before the next `/design-sync`)

1. `.design-sync/shims/live-spot-arcs-age.ts` is a hand port of `getSpotAgeInfo`/`formatSpotAge` from `src/components/map/LiveSpotArcs.tsx`; diff them whenever SpotRow re-verifies.
2. `.design-sync/tsconfig.ds-sync.json` lists every `src/**/index.ts(x)` barrel explicitly. A new barrel directory under `src/` needs a new `paths` entry or esbuild fails with "is a directory".
3. `bash .design-sync/emit-types.sh` must run before `package-build.mjs` or every `<Name>Props` degrades to `[key: string]: unknown`.
4. Changing a `cfg.overrides.<Name>` entry for a component the validator did not flag makes `preview-rebuild.mjs` report CONFIG_STALE; run the full `package-build.mjs` once to re-stamp.
5. Capture is a hard 900×700 crop. Wall chrome previews set `--hc-rail` / `--hc-scale` on the wrapper; do not "fix" them by shrinking the container.
6. `fetch-fonts.mjs` only needs re-running if `index.html`'s Google Fonts link changes.
7. Two upstream bugs are visible in the cards on purpose: #635 (LoadingSpinner `border-3`) and #636 (OnboardingTour modal offset). Re-grade those two after the fixes land.

## 2026-09-08 expansion round (owner ask: PropSphere panels, HamClock view widgets, band health)

- **Set grew 132 → 196.** Added 64 components: 32 PropSphere panels/chips/legends (`src/components/map/`, group `map`), 13 HamClock view widgets (`map/hamclock/`, group `hamclock`), 5 wall controls (`map/hamclock/wall/controls/`, group `controls`), 12 DX/band-health cards (`dx/`), 3 solar leftovers. The 32 wall report modals (`map/hamclock/wall/reports/`) were deliberately left out for a later round.
- **Three new build shims, same mechanism as LiveSpotArcs.** `lib/bundle.mjs`'s paths plugin matches `args.path` exactly regardless of importer, so a tsconfig `paths` key can be a RELATIVE specifier (`"./LocationMarker"`, `"./LiveSpotArcs"`, `"./IonosphericShells"`); those keys redirect every same-named relative import in `src/components/map/` to the shim. Added `shims/location-marker-difficulty.ts` (DIFFICULTY_COLORS/LABELS, getDifficultyColor), `shims/ionospheric-shells-legend.ts` (IONOSPHERE_LAYER_COLORS/NAMES), `shims/ft8-decode-colors.ts` (FT8_DECODE_COLORS, for `src/lib/map/layerLegends.ts`), and `getAgeBadgeColors` appended to the LiveSpotArcs shim. Each is a verbatim port of pure constants; diff against the source on re-sync. A missing named export from a shim surfaces as an esbuild "No matching export" error, so staleness is loud, not silent.
- **Diagnosis recipe for `[SCHEDULER_MISSING]` on every card:** one component transitively imports `three` / `@react-three/*`. The bundle is minified, so walk the import graph instead: BFS from each `componentSrcMap` source over `from "..."` specifiers, honouring the tsconfig paths exact keys, and stop at the first `three`/`@react-three` hit. (Script used this round lives only in the session; it is ten lines of Python.)
- **LayerLegend dropped.** `src/lib/map/layerLegends.ts` imports colour constants from eight different 3D overlay files (SatelliteOverlay, EarthquakeOverlay3D, WeatherAlerts3D, TropicalCycloneOverlay3D, RiverGaugeOverlay3D, FireOverlay3D, GeomagneticFieldLines3D, Ft8DecodeLayer3D); shimming all of them is not worth a 98-line legend. Bring it back only if those constants move to a pure module.
- **SpotHoverPreview was a false alarm:** its only LocationMarker import is `import type`, which is erased; no shim needed for it.
- **GRID_OVERFLOW warnings (39)** are the same set accepted in the first round (dialogs and modals whose fixed positioning is contained by the cell's `translateZ(0)`), plus whatever the expansion waves report. Non-blocking; only apply `cardMode` overrides where the sheet actually shows a broken card.
- **Wave learnings (expansion X1 HamClock view, folded 2026-09-08):** `HamClockLayerChips`' root is `hidden lg:flex` (real header behaviour), invisible at the 900px capture width, so `overrides.HamClockLayerChips = { cardMode: "single", primaryStory: "Default", viewport: "1200x200" }`. `HamClockDialog` (open) stayed contained in both cells, no override. `HamClockContestsPanel` / `HamClockDxpeditionsPanel` never resolve their RSS fetches inside the capture's networkidle wait, so the cards show the styled "Loading …" state, graded good as honest network-widget states.
- **Wave learnings (expansion X2 PropSphere panels A, folded 2026-09-08):** two more floor cards with no forcing prop, previews deleted: `ContestRatePanel` (returns nothing unless `useContestStore().activeSession` has QSOs and the `contestWatch` flag is on) and `OptimalBandsPanel` (returns `null` while `useMapStore().target` or `useUserStore().station` is null). `ConditionsPill` and `MapStatusChip` render a real styled node in their no-data branch so they stay. `PanelMiniStrip` is a genuinely 40px-wide docked strip; the empty space beside it on the sheet is the cell, not overflow (wrapper width 40). Only `MUFLegend` is previewed; `MUFLegendCompact` shares its file and is not a bundle export.
- **Wave learnings (expansion X3 PropSphere panels B, folded 2026-09-08):** floor card `WatchStatusPill` (all state from `useWatchStore()`, unseeded, no props; no preview file). `PropagationForecast` / `PropagationForecastMini` read station/target from stores with no props, so one honest no-QTH state each. `SolarSnapshot` and `SatellitePanel` show their "unavailable" states because solar/TLE fetches never resolve in the capture browser. All portal/overlay components in this set (ClusterDetailPopover, PinFlyout, SpotDetailsModal, SpotHoverPreview) stayed contained without overrides.
- **Wave learnings (expansion X4 dx/band health, folded 2026-09-08):** floor card `ConditionMatchCard` (network hooks fail fast in the sandbox, `isLoading` false, then `return null` when SFI or the logbook is empty; preview deleted). `BandVerdictDetailsDialog` (open, portal via AccessibleDialog) stayed contained at the default viewport, no override. BandVerdictPanel, ClusterPulseCard, HistoryCard, LogStatsCard, SpotStatsDashboard, WSJTXStatusPanel are hook/store-driven with near-zero props and render the app's real empty/disconnected states, same pattern as the wall tiles.

## 2026-09-08 round 3 (owner ask: operator profile, 24 h forecast + NowCast, band conditions, path info, globe layers/legends, Pro-mode docked menus, notices, both tickers)

- **Set grew 196 → 235.** 40 candidates from an import-graph inventory (`.design-sync/.cache/round3-additions.json`); `SatelliteDetailModal` (`map/layers/`) was dropped after the wave found its emitted `.d.ts`/`.prompt.md` described the unrelated same-named `src/components/satellites/SatelliteDetailModal.tsx` (ts-morph resolves `<Name>Props` by name, not by path) — a wrong contract is worse than no card. Excluded up front for Three.js chains: LayerLegend (still), CompassRose, Ft8CycleRadar, Ft8GridHeatmap, Ft8SpotterOverlay, PathPointHitArea. Dead/unwired and skipped: StyleSelector, MiniMapNavigator, SatMatchPanel.
- **Re-sync driver works as documented:** `_ds_sync.json` from the project saved to `.design-sync/.cache/remote-sidecar.json`, then `resync.mjs --remote <that file>`; it reported 196 unchanged / 40 added / 0 changed. Render hashes are over the preview markup, so a CSS-only change in `src` (the #640/#641/#643/#645 fixes) shows as `upload.styling` + `upload.bundle` with zero "changed" components — re-grading such cards needs an explicit `package-capture.mjs --components … --force`, which is what was done for the 16 affected cards.
- **New floor cards (no forcing prop, previews deleted):** AlertGlowOverlay (settingsStore flag + alertsStore), AlertToastContainer (alertsStore queue, no `alerts` prop; SpotAlertToastContainer has one and is authored instead), ResearchAttemptControl (gated by the build-time `VITE_PROPAGATION_RESEARCH_*` env flag, always null here).
- **Honest empty states kept:** DXSpotList and OperatorProfile (store/bridge-driven, no data props), SatelliteFilters (TLE fetch resolves empty). EmergencyTickerBar is non-deterministic: it renders only when the live SWPC feed returns alerts during capture; no prop to inject entries.
- **Overrides added this round:** SpotAlertToastContainer single 900x400 (fixed-position portal, bottom-right of the canvas), PropagationForecastModal single WithNowCast 900x1600 (xl modal taller than the default cell), ProToolbarRibbon single Expanded 1800x300 (component reads `window.innerWidth`; below 1600px it is always the compact branch), EquipmentSection single Default 900x1150 and EquipmentHeroCard single Transceiver 900x1300 (the vertical tile stack from #643 made both taller than the cell; `column` mode still clipped because cells have fixed height).
- **Persisted-store leak between captures:** stories that click a band tile or a watch quick-start button mutate a persisted Zustand store (localStorage) and leak into later captures in the same browser session. Rule from R3-A: author controlled-prop variants instead, or capture such a component in its own isolated invocation after everything else (BandModeModal was).
- **Re-grade after the DS fixes (batch R3-R):** all 16 affected cards good; SectionHeader gained a `StackedNarrow` story (300px, `layout="stack"`) and Grid a `NarrowSingleColumn` story (320px) so the narrow variants are visible on the cards, not only in the app.
