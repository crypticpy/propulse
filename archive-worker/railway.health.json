{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "archive-worker/Dockerfile",
    "watchPatterns": [
      "archive-worker/**",
      "supabase/migrations/202607190*.sql"
    ]
  },
  "deploy": {
    "startCommand": "python -m propagation_archive health",
    "cronSchedule": "*/15 * * * *",
    "restartPolicyType": "NEVER"
  }
}
