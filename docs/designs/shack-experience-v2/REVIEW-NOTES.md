# Concept review notes

Seven original concepts were generated with built-in ImageGen and visually inspected. Exact prompts are linked in manifest.json. These images propose composition and visual direction; no generated screenshot is implementation or accessibility evidence.

- Reuse the actual application header. Generated wordmarks, dates, names and demo gear specifications vary across images and are not authoritative.
- In 01, attach cables to exact named domain ports, even where the rendering appears to land near a device body. FLOWS.md defines the topology and switch state.
- In 04, the faint private album behind the viewer represents an owner session. Public visitors only receive authorized media. The fictional operator identity varies between concepts.
- In 05, written requirements also require condition and price filters even where the sketch does not show every filter.
- In 06, the generated contact-method selector illustrates placement. The written proposal uses an authenticated inquiry relay; a public profile contact link is not silently enabled.
- In 07, disable Earlier on the first photo and Later on the last photo; the generated control states are inaccurate. A current cover must say Cover rather than offer Set as cover again. Uploads and URL imports preserve other unsaved gear fields. Crop, removal, alt text and error states are requirements even where this single image omits their controls.

Soft text colors, all four themes, text scaling, contrast, keyboard/screen-reader behavior and actual operator comfort require implementation checks. No simulated participant feedback or measured accessibility outcome is claimed.

## Package verification

- All 40 SX2 requirements have exactly one primary issue; all seven images and exact prompts exist; local Markdown links resolve; the new dependency graph is acyclic.
- Standalone gallery checked in a disposable Chromium context at 1440×1000 and 390×844: seven images load, enlargement/previous/next/wrap work, Escape restores focus, no horizontal overflow or browser errors. This checks the concept gallery, not the proposed application screens.
- `npm run lint` and `npm run build` passed against baseline `0b4cf402` plus these documentation artifacts. No app source or database migration changed.
- An independent agent reviewed requirements, flows, delivery and overview. Its ID-collision and public-photo-credit clarifications were incorporated. Operator studies and production feature verification remain future implementation gates.
