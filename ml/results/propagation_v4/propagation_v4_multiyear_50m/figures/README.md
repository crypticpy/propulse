# Figures

Charts are rendered and embedded from `REPORT.artifact.json` by the canonical portable report builder. Standalone exports must be extracted from that same validated artifact so the HTML and public figures cannot diverge.
