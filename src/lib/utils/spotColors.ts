/** Compatibility entry point; spot color authority lives with domain palettes. */
export type { SpotColorMode } from "@/lib/colors/palettes/spots";
export {
  MODE_COLORS,
  MODE_INK_DARK,
  MODE_INK_LIGHT,
  SPOT_REPLAY_COLOR,
  MODE_COLORS_TAILWIND,
  getModeColor,
  withAlpha,
  inkOnFill,
  modeInk,
  getModeTailwindColor,
  BAND_COLORS,
  getBandFromFrequency,
  getBandColor,
  SNR_COLOR_STOPS,
  AGE_COLOR_STOPS,
  getSnrColor,
  getAgeColor,
  getSpotColor,
} from "@/lib/colors/palettes/spots";
