/** Stable type-only entry point for SP-04–SP-09 consumers. */
export type {
  SpotPresentationPreferences, NormalizedSpotReport, SpotLocation, StationEndpoint,
  NormalizedMode, ModeSelection, ClusterGroup, SpotSceneModel, SpotSceneCounts,
  PathDescriptor, PathPointDescriptor, PathAppearance,
} from "@/lib/views/spotContracts";
